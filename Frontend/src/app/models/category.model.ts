
export class Category {
    Nom: string;
    Type: string;
    Photo: string;
    constructor(Nom: string, Type: string, Photo: string) {
        this.Nom = Nom;
        this.Type = Type;
        this.Photo = Photo;
    }
} // modèle pur cération de l'objet catégorie
